# 2026-01-14 会话记录

## 变更摘要

- 根据 `/GPFS/cuizaixu_lab_permanent/xuhaoshu/ABCD/logs/freesurfer` 日志完成标记，更新 `outputs/logs/freesurfer/sublist_freesurfer_from_csv.txt` 为仅包含未完成的 5 个被试。
- 在 `AGENTS.md` 增加规则：修改代码或文档后必须通过 git 提交并自行撰写提交信息。
- 更新 `README.md` 和 `docs/workflow.md`，补充形态学提交与汇总表说明。
- 更新 `docs/reports/implementation_specification.md` 与 `docs/methods.md`，锁定 CLG-ODE 的拓扑条件输入定义、SC 重建损失分解、形态学 Z-score 粒度、协变量集合与多起点配对采样策略（Topo 不作为训练 loss）。
- 补充锁定细则：ECC 三角形计数采用阈值二值图的 clique complex 2-simplices（稀疏矩阵乘法实现）；连接存在性/强度共享内积分数并通过可学习标定参数输出；SC 强度协变量 `s` 在原始权重域计算并可选添加 `s_mean`。
- 明确 `s_mean` 为正边平均强度：`log(mean(A_ij | A_ij>0) + ϵ)`，用于补充 `s=log(∑A_ij+ϵ)` 的总强度信息。
- 补充集群 GPU 使用文档入口：在 `AGENTS.md`、`docs/README.md`、`docs/workflow.md` 与根目录 `README.md` 增加对 `docs/cluster_gpu_usage.md` 的引用，并提供本仓库 CLG-ODE 的容器运行示例。

## 细节

- 未完成被试：`sub-NDARINV38MDKPJC`、`sub-NDARINVD11XG6G3`、`sub-NDARINVH7TM17PE`、`sub-NDARINVTD7KHM6F`、`sub-NDARINVY6FE3R8A`。
- 失败原因汇总：`nu_correct` 阶段缺少 `MNI/Startup.pm`（Perl 模块路径 `@INC` 未包含 FreeSurfer 的 MNI Perl 库）。
- `AGENTS.md`：在 Base workflow 中新增提交要求，避免无提交的变更。
- 文档：统一 README 中的提交示例结构，并在 workflow 中标注 `data/processed/table/` 的统计文件与用途。
